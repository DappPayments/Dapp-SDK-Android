<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.dapp.sdk.wallet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".scanner.DappWalletScannerActivity"></activity>
        <activity
            android:name=".gui.DappReaderActivity"
            android:configChanges="keyboard|keyboardHidden|screenSize|screenLayout|orientation"
            android:label="@string/title_activity_reader"
            android:screenOrientation="portrait" />
    </application>

</manifest>